# Copilot Instructions

Follow the repository guidance in `AGENTS.md` at the repo root. In particular, use pnpm, keep changes scoped to the relevant package, start the seeded Starknet devnet before broad tests that need port 5050, and run the focused package checks before handing off.
