# useExplorer

Access the current explorer, should be inside a `StarknetConfig`.

## Usage

```ts twoslash
import { useExplorer } from "@starknetfoundation/starknet-start-react";

const explorer = useExplorer();
```

## Returns

### explorer

- Type: `Explorer`

The current explorer, e.g. voyager.
